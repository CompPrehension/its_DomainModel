# Loqi Syntax Highlighting

This directory contains editor syntax packages for Loqi:

- `vscode/` - VS Code extension for `.loqi` and `.tpg` files.
- `sublime/` - Sublime Text package for `.loqi` and `.tpg` files.
